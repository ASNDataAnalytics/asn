# asn 0.0.2

* Added ASN Typst template accessible using `asn_use_template()`. 

# asn 0.0.1

* `get_labs()` was renamed to `get_labels()` to avoid a namespace conflict with `ggplot2::get_labs()` introduced in ggplot2 version 3.5.2.

