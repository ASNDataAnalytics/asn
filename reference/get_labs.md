# Retrieve labels from labeled data frames from SPSS `.sav` files

**\[deprecated\]**

`get_labs()` was renamed to
[`get_labels()`](https://asndataanalytics.github.io/asn/reference/get_labels.md)
to avoid a namespace conflict with the
[`ggplot2::get_labs()`](https://ggplot2.tidyverse.org/reference/labs.html)
function introduced in version 3.5.2.

## Usage

``` r
get_labs(x)
```
